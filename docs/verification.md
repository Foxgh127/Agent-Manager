# 1.1.0 验证记录

本机验证日期：2026-09-10。

- 完整后端回归：**1770 passed、5 skipped、243 subtests passed**。
- 前端：**65 项通过**，生产构建通过；实际 React 组件的默认额度卡片截图确认只有一行。
- 回环 HTTP 双上游验证 OAuth/API 池、会话绑定与重启、限流等待、在途负载、工具/推理流、身份变化拒绝、已报告消耗不重放及异常连接关闭。
- 计量验证原始事件捕获到持久化、不同请求档位分组、旧 schema 迁移、272K 边界、缓存防重复、条件区间与可比历史限制。
- 导入验证短期 OAuth、API 文本/嵌套、单项错误隔离、每层管理凭据排除与数量/深度限制；未使用真实令牌。
- 在独立虚拟环境中构建 EXE，验证新增模块、版本和资源；复制到中文及空格目录，从其他工作目录启动帮助入口成功。
- 本轮未运行收费模型或对真实账户做容量削减实验。数值是 API 等值及条件范围，不能当作账单或因果证明。

云端最终矩阵与提交对应结果见 [GitHub Actions](https://github.com/Foxgh127/Agent-Manager/actions/workflows/release.yml)。

## 1.0.1

本机验证日期：2026-09-10。所有功能验收使用临时配置、模拟用量和隔离进程，不调用收费模型、不关闭用户 Codex。

- 后端整套回归：1668 passed、4 skipped、231 subtests passed；后续 USD 历史和容量边界、清理范围专项通过。
- 前端：61 项测试通过，生产构建通过。覆盖激活终态刷新、启动请求合并、历史样本与美元状态显示。
- 真实独立 Python 进程重开后读取已保存的 5 个区间，随后继续累积至 6 个；同账号重复激活保留计数 epoch。
- 缩小文件容量上限后优先裁剪最老历史，保留当前账号并继续保存新样本；新增未知用量不进入美元额度拟合。
- 4 个并发冷读取只执行一次 CLI 探测；验证缓存到期、安装变化、主动刷新和失败退避。缓存命中用例不运行 CLI。
- 原生子代理策略无管理标记仍健康；生成策略确有变化时继续报警。配置读写锁和启动完成竞争场景通过。
- Windows EXE 的资源、模块、版本 1.0.1 与发布代次 1 通过校验；复制到带中文和空格的其他路径后，从不同工作目录执行帮助入口成功。
- 用实际 React 组件和模拟数据生成独立预览，核对已校准、等待新额度和首次采样三种布局。
- 清理测试验证预览不删除、自选诊断路径不波及其他材料、拒绝源码/外部路径。当前主机无法创建目录符号链接的用例跳过，链接保护逻辑仍保留。

最终提交的 Python 3.11 / 3.14 完整矩阵与发布结果见 [GitHub Actions](https://github.com/Foxgh127/Agent-Manager/actions/workflows/release.yml)。本地隔离速度测试不能代表所有设备的实际启动耗时；价格等值也不代表真实账单。

## 1.0.0

本机验证日期：2026-09-09。

- 后端完整回归：1647 passed、4 skipped、225 subtests passed。
- 前端：58 项测试通过，生产构建通过。
- Windows 安装包：内置资源、包导入、版本1.0.0、发布代次1校验通过。
- 将EXE复制到含中文与空格的不同目录，从另一个工作目录执行只读帮助入口，正常退出。
- 通过实际React组件预览确认维护区右侧文字不再裁切，手动备份、分类、删除和底部按钮可用。
- 配置保留策略、原件保护、路径/身份变化、恢复冲突均使用临时目录验证。
- 网关普通/包装响应取消场景各重复验证，保持2秒并发槽回收门限；不放宽超时。
- 会话目录缺项采用原生重建请求，测试覆盖结构白名单、扫描状态、备份与回滚。真实目录恢复仍需结束Codex、执行修复、重启后复诊。

真实网页登录站点可能继续要求人工验证；不绕过站点挑战。没有运行收费模型效果评测，也不将Windows单机结果当作所有设备都已验证。

本轮另验证：备份恢复记录异常时保留原件、目录迁移后的完成记录、响应包装身份指纹，以及 www/主域地址差异时的新连接提示。

云端矩阵和发布结果以 [GitHub Actions](https://github.com/Foxgh127/Agent-Manager/actions/workflows/release.yml) 为准。
