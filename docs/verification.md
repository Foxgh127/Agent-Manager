# 1.1.5 验证记录

验证日期：2026-09-11。

- 完整后端回归：1939 passed、5 skipped、270 subtests passed。
- 前端：73 项测试通过；覆盖 2×2 布局、快捷方式定位、仅退出前的实时确认和后台退出失败反馈。
- 使用隔离进程与时钟验证批量精确 PID 关闭和统一时间预算；独立组件并行停止，必要组件仍在退出或失败时不报告完成，显式重试不会重复恢复配置。
- 对照测试中，64 个模拟进程、每个关闭命令耗时 200ms 的路径由 64 条命令、12.8 秒模拟时间降为 2 条命令、0.4 秒；九个独立 40ms 清理操作的实测中位数约由 363ms 降为 42ms。这些是测试夹具结果，不代表真实退出耗时保证。
- 快捷方式验证覆盖显式当前用户令牌、被覆盖的 USERPROFILE、Unicode 原生读写、落盘回验和桌面刷新通知。实际桌面的既有 Agent Manager 快捷方式已核验并刷新，未重复创建。
- 新图标统一导出圆角透明 PNG 与十种尺寸 ICO；应用内使用内容指纹文件名，避免旧图标缓存。打包检查确保界面和托盘图案一致。
- 进程退出与重启测试使用隔离替身，没有关闭正在运行的 Codex 或管理器。
- 安装包检查资源、模块、版本与发布代次，并在中文及空格目录从不同工作目录验证帮助入口。

真实系统扫描和配置恢复仍是必要步骤。无法确认安全完成时保留恢复界面，不靠强制退出跳过校验。

最终提交的云端矩阵见 [GitHub Actions](https://github.com/Foxgh127/Agent-Manager/actions/workflows/release.yml)。
