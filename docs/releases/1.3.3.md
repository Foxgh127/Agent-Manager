# Agent Manager 1.3.3

- 接入 modelprint 响应头指纹解析与 LoongPort 被动协议观察，在正常 JSON/SSE 响应上采集，不发送额外探测请求。
- 用量详情区分上游声明、协议指纹、官方样本关联候选与无法确认的型号；多候选明确显示歧义，并可查看开源规则来源。保留历史计量兼容，筛选与导出采用相同模型显示名称。
- 统一首页、前后端、Windows 文件属性和资源版本为 1.3.3。更新包提供两种历史文件名的同字节兼容资产，附带完整更新 manifest 和 SHA-256。
- 修复旧公开版本缺少 manifest 时的无更新误报、更新文件名识别、模型目录模板缺失字段、会话列表分页/批量操作/本地置顶及重命名回归。
- 修复 API 来源启动检查、官方账号环境隔离和商店包启动环境传递；Provider 余额作为带时间/过期状态的快照展示，真实上游 Codex 额度头继续透传，不将法币余额冒充 Codex Credits。

指纹相同仅提供关联线索，不能证明模型权重；没有独立参考的精确型号保持未确认。API 原生额度面板是否可见仍取决于上游返回与 Codex 客户端支持。
